version: '3.8'

services:
  aigra-ops:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aigra-ops-platform
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=production
      - LOG_LEVEL=info
    restart: unless-stopped
